use ratatui::Frame;
use ratatui::layout::Rect;

use crate::app::App;

/// Render the workspace list in the left panel
pub fn render(_frame: &mut Frame, _area: Rect, _app: &App) {
    // TODO: T08 — implement workspace list widget
}
