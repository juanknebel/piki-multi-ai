use ratatui::Frame;
use ratatui::layout::Rect;

use crate::app::Workspace;

/// Render the workspace tabs bar
pub fn render(_frame: &mut Frame, _area: Rect, _workspaces: &[Workspace], _active: usize) {
    // TODO: T19 — implement tab bar widget
}
