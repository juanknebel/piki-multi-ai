use ratatui::Frame;
use ratatui::layout::Rect;

use crate::app::App;

/// Render the changed files list in the left panel
pub fn render(_frame: &mut Frame, _area: Rect, _app: &App) {
    // TODO: T13 — implement changed files widget
}
